# 任务列表：RBAC 部门数据隔离、调拨与自动化部门管理

- [ ] **基础架构与展现对齐** <!-- id: 0 -->
    - [ ] 强化 `Dashboard.tsx` 和 `AssetList.tsx`：建立 ID 至名称的映射翻译，确保操作员看到的不是数字。
    - [ ] 修复统计逻辑：确保后端 `GET /api/assets` 的过滤逻辑与前端 Dashboard 的统计数字完全匹配。

- [ ] **融合式部门管理 (集成于人员管理)** <!-- id: 1 -->
    - [ ] 后端：新增 `POST/PUT/DELETE /api/departments` 接口。
    - [ ] 后端：新增 `POST /api/departments/merge` 接口，支持输入新名称并原子化更新资产关联。
    - [ ] 前端：修正 `PeopleManagement.tsx`，在部门勾选列表添加“新增/更名/删除”入口。
    - [ ] 前端：实现多选后的“合并”功能，弹出对话框要求输入新部门名称。

- [ ] **逻辑升级：自动创建部门** <!-- id: 2 -->
    - [ ] 修改 `src/server/routes/assets.ts`：手动新增资产时，支持 `dept_name` 为新字符串时自动插入部门并关联。
    - [ ] 修改 `src/server/routes/excel.ts`：Excel 导入逻辑同步支持自动识别并创建新部门。
    - [ ] 修改 Excel 导出：通过 SQL `JOIN` 将 `dept_id` 转换为 `departments.name` 输出。

- [ ] **跨部门调拨审批流** <!-- id: 3 -->
    - [ ] `Scan.tsx` / `AssetDetail.tsx`：完善“申请调拨”流程，优化“跨部门资产”的只读展示。
    - [ ] `Dashboard.tsx`：为管理员实现“待办调拨”列表的实时审批交互逻辑。

- [ ] **前端：权限 UI 深度扫描** <!-- id: 4 -->
    - [ ] 权限拦截：确保操作员无法直接通过直接修改 URL 访问人员管理或审计日志。
    - [ ] 标题翻译：Dashboard 标题旁显示的“管辖范围”需正确翻译为部门名称字符串。

- [ ] **验证任务** <!-- id: 5 -->
    - [ ] 验证：手动新增一个从未出现过的部门资产，检查部门表是否自动新增。
    - [ ] 验证：导出 Excel，确认“部门”列显示的是中文名称而非 ID。
    - [ ] 验证：合并部门 A 和 B 为 C 后，原 A/B 部门的资产是否全部变为 C。
